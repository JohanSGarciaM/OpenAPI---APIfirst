openapi.yaml is the validator file, all the schema is defind in it and the application is running the src/index.js main file

to see the application specifications run:
    - npm run dev

and go to localhost:3000/docs in your browser
